# Contact/pass interruption integration

This checkpoint corrects three port omissions in the real knockdown caller:
the original action35 branch may keep possession; action36 gives a non-owner
victim its vertical impulse; an actual owner drop updates0910 to the ball record.
Original pass metadata may survive the knockdown and recovery. The regression
now distinguishes the active mode15 executor from retained pass globals and
checks the interruption/recovery transition separately. No original bug is fixed
or reclassified, and no native expected values or image goldens are replaced.

Candidate freeze `c1-pass-interruption-20260831/build/c1-freeze-v1.json` has
SHA256 `b3653771a3384bfa705cf4255d424a912b726fc7f1095dcd3ab6ef3533ef5253`.
Root verified all581identities before copying the11source/test/document files
byte-for-byte onto11f111c. Exact-byte attributes preserve those reviewed files;
the CPU test's larger textual diff includes retained Windows line endings.
The implementation and source-domain details remain in the unchanged
[candidate report](pass-interruption-c1.md).

## Root verification

Evidence is retained under `build/c1-integration-v1`, with fresh build manifests,
commands, exit codes and artifact hashes. No old object or executable is reused.

| Check | Root result |
|---|---|
| Actual-body contact probe,39translation units including nba_tipoff.c in the probe | Fresh MSVC /W4 /WX build;9,216controlled original-byte-pinned cases pass |
| Real neutral-input actor loop | One41876interruption,15due recovery passes, one41908return pass |
| Regression guard checks | Six positive and29negative cases pass |
| Fresh40-source CLI,63,800frames | Exit0; entire trace matches the frozen candidate byte-for-byte |
| Final interruption guard over that trace | Oneentry/onerecovery, no unfinished episode; no receiver-only cancellation reached |
| Fresh40-source HUD runtime | 120pause holds,182formation holds,7,630dead-ball advances,1,942dead-ball holds,32,001live steps and two final-minute views pass |
| Fresh40-source new-match runtime | Unchanged native startup projection and two C return journeys pass; no native second-match timing claim |
| Final combined CPU regression | Gameplay/state/pass/receiver/sustained-motion checks pass; exit1 at the same existing frame600RGB golden; subsequent source-marker checks do not run |

The root trace SHA256 is
`f69d2637d2bf2da6c9fcebfc90f70bbb2f47acc869aa70df9f38115f715e8236`.
All tests use the264-entry HUD pack SHA256
`f564c29612928984002ed3f0389d317de639fff122baf61a7bc9ecaef2a6be09`.
Independent QA accepted this bounded repair after rehashing the581identities,
rebuilding the candidate and unchanged baseline, reproducing936baseline vector
failures, running a fresh63,800-frame executable, streaming the retained trace,
and rejecting five additional erased/redirected identity cases. Receipt:
`checkpoint-qa-20260831/build/c1-independent-audit.md`, SHA256
`d5ddd5218980ee804b72d75032d1468a34eb5d75ebecb486f6805b65b09ea3a7`.
Checkpoint screenshots were published from clean integration commit
`facd818e6e798f6546c00aeaa5dcbb84835a4cf4`; see the dated receipt in
`implementation-checkpoints-20260831.md`. The thirteen standard views are
unchanged, and the supplement shows the41875/41876/41908 contact sequence.

## Limits and next work

The natural original14,000-court-frame capture reached no successful contact.
This repair has independently source-grounded controlled coverage and real C
caller regression coverage, not natural original knockdown parity or full CPU,
register, direct-page, timing, classifier, injury or animation-parent parity.
The controlled receiver cancellation and non-owner action36 cases must not be
reported as naturally reached by the63,800-frame C journey.

The final root broad test reaches the existing frame600RGB golden failure after
the gameplay/state/pass/sustained-motion checks. Actual RGB SHA256 is
`b555afbcbfeb868b1a56b255ea15259214d9d9c49044d294b34c7d34c400853f`.
Its exact command, exit1 and logs are retained in `broad-regression/receipt.json`.
Retained pre-C1 and candidate
executables render identical600/1300BMPs with the same pack, attributing that
image failure to the prior baseline. No full-suite pass or whole-game completion follows
from this checkpoint. Shared aliases/B468, period restart/draw ordering, normal
bootstrap/timing, remaining human controls, retail modes and disk saves remain
on the completion plan. Main and the desktop executable are unchanged.
