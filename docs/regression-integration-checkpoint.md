# Audited regression integration

The independent reports now accept the bounded checkpoints described in the
frozen gameplay85, startup fixture, closure driver and tipoff image documents.
Those documents retain their original pre-review status wording because their
bytes are part of the evidence freezes. The separately named
`completion-*-independent-audit.md` reports record the final verdicts.

The startup repair isolates synthetic boosted-pass test data from the actual
chosen home team. All 841 home/visitor pairs initialize; all 812 previously
successful complete owned states remain byte-identical. It changes no original
rating, gameplay branch or user selection. The new build gate covers every pair
with quarter settings rotating across pairs, not all four settings per pair.

The endurance hashes retain all three 16,000-frame runs and semantic guards.
The closure driver supplies actual released Setup input between presses and
asserts the return row instead of assigning it. Its two 6,000-frame runs agree;
controlled old-source/configuration builds reproduce the old digest exactly.
Canonical team/rank source and the separately accepted layout1 constructor
explain the changed C outputs. All native witnesses and failed attempts remain.

The tipoff image update changes only three C RGB expectations and comments.
Single-source controlled builds reproduce every old and new pixel. The stale
static WEST/ORLANDO score panel remains an unresolved port gap, not an original
game bug or an accepted live scoreboard.

Full suite v15 passed these startup, endurance and closure gates before the old
tipoff image expectation stopped it. The updated complete tipoff test passes
separately. The later CPU trace still fails a camera observation assertion and
requires investigation. No full-suite, human-play, scheduler-phase or whole-game
completion is claimed. Production main and the desktop executable are unchanged.
