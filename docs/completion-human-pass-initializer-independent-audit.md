# Independent AB2D prefix/first-AC50 review

PASS for the bounded checkpoint at
`completion-controllers/build/human-pass-initializer/freeze-v1.json`, SHA256
`7412276c4ea9b2fec043d81fe1a779e5f0ba76113bb19cbc085ffced15d3ef4d`.
This accepts DF7A selection through AB2D prefix and geometry to the first
AC50. It does not accept the AC50 branch or later initializer/action children,
full AB2D closure, pass release, production scheduling or normal human enable.

All205 frozen source/dependency/object/artifact/prior-freeze identities were
independently checked before review and rechecked unchanged afterward. The
eleven new files and explicit source dependencies are copied exactly under
`build/pass-init-audit-v1/source`. Module source SHA256 is
`9d621f3894e3ed31de64a4d7210651d10a06709efc863c3f71d648fd6c66138f`;
header `cbdeaf97144b2d5318639adfacf098e9c360c507a52de759c54706352954ab78`.
No original capture or prior checkpoint changed in this audit.

A fresh MSVC /W4 /WX build compiled the new module, unchanged pass selector
and probe, linking the auditor's previously freshly built accepted controller
dependencies read-only. Fresh probe SHA256:
`267f7bd2f420b75762b6bb0bf7f8c419c8cecbba1c3c3ebb515af59a3ec27e7c`.
Both untouched native captures pass216,466 values:25 selections;25 each of
cancel, prefix, fine, geometry and combined-chain checks; plus41 F1C1 metrics.
The left DF7A entry166/court1471 through ready183/court1472 crossing remains
included, both as selection and combined-chain observations. No call is
discarded because it crosses a frame or follows a less common branch.

I checked all20 reference range/artifact identities against the actual original
ROM SHA256 `2115c39f0580ce19885b5459ad708eaa80cc80fabfe5a9325ec2280a5bcd7870`,
read AB2D/geometry/B538 and F3C3 Ghidra listings and their C translations,
and inspected the actual capture/runner and probe. The prefix follows source
ordering: B538 upper cancellation, actor+00 descriptor lookup, distinct C2
publication to0942, receiver identity to0946, active/inbound flags, then receiver
mode10/timer40. The descriptor's high word remains full16-bit, not a truncated
bank byte. Any nonzero upper lock cancels; lower animation channels, queued
data and unrelated actor fields stay unchanged.

Geometry implements the original four signed passer velocity shifts and
three receiver shifts,16-bit coordinate sums/differences, separate F3C3 fine
direction, and sequential N-based distance-band comparisons. The final
coincident result is fine16/coarse8/relativeFFFF. It is not an early exit from
prefix effects. Airborne state is inspected only by later source branches,
so the new prefix correctly does not reject it. Unusual wrap behavior and
negative-lock behavior remain source-commented rather than normalized.

Independent `test_human_pass_init_source_audit.py` passes609 controlled cases:
576 fine-direction boundary pairs using the actual ROM table; eight complete
prefix state comparisons for locks0/1/8000/FFFF and live/inbound state;24
geometry cases covering all band thresholds, signed velocity edges, position
wrap, coincident targets and wrapped distance comparisons; and NO_RECEIVER
skipping all initializer writes even with an invalid descriptor index that
would reject an actual prefix call. The latter still publishes090E|10.
Every typed persistent region is compared, not only the edited words.
These cases are controlled source-contract coverage and are not counted as
natural native witnesses. The natural captures cover grounded/noncoincident
endpoints, locks0/1 and bands0/6/12/18/24 only.

Verifier SHA256:
`0b9056eb46f23db9ae4cf08c65caa215ddcf1d3da4ad354dae6e1963f86ab207`.
Its entry-only C inputs, exact typed outputs, mandatory paired stages,
first-AC50 boundary, raw-backed metadata and private capture attestation were
inspected. Native after-state never enters the C module. The combined replay
starts at DF7A entry, so it does not substitute the recorded receiver choice.
Geometry's expected predicted dx/dy are read only by the verifier from the
native fine-entry snapshot; the C geometry computes its own inputs from actor
prestate. All69 local and39 unchanged independent integrity mutations reject.
Clock anchors bind the pinned diagnostic route, not production phase.

A separate fresh probe relink with the independently accepted repaired F34F
helper also passes both captures with byte-identical C responses. The frozen
initializer's original F34F dependency still has the historical extreme-word
defect described in the switch rejection; use the separately accepted helper
repair when composing these modules. F3C3 is a distinct routine and passed
the independent full-word boundary checks above.

The original failed old-initializer investigation and capture/reference
attempts remain preserved. This audit does not clear the recorded nine old
initializer mismatches or infer a repair for later animation/action branches.
Do not append the old initializer after this new prefix: that would repeat
prefix mutations and bring back unverified later behavior. Return CPU state,
stack restoration and unlisted volatile scratch remain outside this semantic
stage; a later continuation must establish their required calling contract.

Evidence under `build/pass-init-audit-v1` includes freeze/reference/final
rechecks, fresh build logs and native reports, `local-tests`,
`independent-integrity`, `source-contract-v2/report.json`, and separate
fixed-helper native replays. The original608-case report remains beside the
609-case extension. Parent integration and broader production regressions
remain separate from this bounded acceptance.
