# Accepted human pass saved-state return

The AF4D initializer frame, E09C selector frame and E3E6 human B-mask restore
are independently accepted to the E3E9 boundary. Root rehashed277 identities,
copied14 reviewed files, and rebuilt only the new C module/probe with MSVC
/W4 /WX. The probe needs no asset loader or older gameplay objects.

The two native routes reproduce189,100 values across25 calls. All112 local
and25 unchanged independent malformed-evidence cases reject, including saved
frame corruption, incorrect numeric types, duplicate fields and any stderr.
The original frame crossing and saved words are preserved. The independent
source diagnostic additionally matches55 ROM PCs,75 saved frames and175 return
segments. This is not complete initializer, pass-launch or human-play acceptance.

The first root runner successfully verified the left route, then requested a
nonexistent right `selection2-v2` directory. That failure is retained. A separate
retry uses the actual frozen `selection2-v1` capture and passes its native and
protocol checks. No capture, expected state or production source changed for
the retry. Records are under `build/return-integration-v1`.
