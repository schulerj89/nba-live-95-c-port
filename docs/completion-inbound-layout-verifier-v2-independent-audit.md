# Inbound verifier revision 2 independent acceptance

**PASS for the bounded composite:** the unchanged layout-1 source repair and C-only Mode 1 refresh reviewed in `completion-inbound-layout-independent-audit.md`, plus the separate repaired verifier. The rejected original verifier and its nine accepted-invalid cases remain preserved.

New freeze `completion-owner/build/inbound-layout-verifier-freeze-v2.json` SHA256 `81eb9a72db63e0f5cc24b192d1f15e02cba47bf360ef89274b9feeec417fda7e` contains twelve new identities and the prior 363-file freeze reference. All thirteen were independently read and checked. The reviewed new verifier is `19c87e2ec0b6ef8e05db0afb1dae3804c96f78bbd0482fb72a7cca1b7e09b5ca`. Private exact copies/results are under auditor `build/inbound-layout-audit-v2`; production source and native captures did not change.

The repaired verifier requires exact manifest/isolation schemas and method, actual integer controlled addresses, pinned source identities at the actual copied script/runner/helper/executable paths, one coherent source-defined constructor destination tied to the raw layout word, and canonical six-word stdout with no stderr. The declared copied source paths now bind to the same files checked in the artifact inventory. Actual settings and final saves are independently reread using an isolated copy of the declaration; observed results must equal that declaration. The earlier route, word-domain, CPU/WRAM injection and raw output checks remain intact.

The unchanged independent nine-case tool now rejects every previously accepted invalid view. All 33 original local cases also pass. The same freshly compiled corrected probe passes all 54 words from the nine original captures. Its complete new-verifier records equal a final rerun of the original verifier on the same private executable, including all input/output values and manifest identities. The initial private baseline records had a different PE identity after a later private relink; comparison first caught this metadata difference. The final comparison uses the same executable and passes; no native or source value changed. Both initial and final records are retained.

This acceptance is limited to genuine-entry source-contract replay (one natural, eight explicitly controlled cases), the independently reproduced C trajectory attribution, and the verification protocol. The PC list check establishes constructor consistency, not every executed instruction or elapsed CPU time. It does not establish arbitrary full-word layout inputs, whole-game native trajectory, whole-frame parity, human control enabling or full-suite completion. Layout 4's original unclamped result remains preserved.

The separately reviewed shot-state fixture acceptance is recorded in `completion-shot-state-fixture-independent-audit.md` and is not silently included in this source freeze.
