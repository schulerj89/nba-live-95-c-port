# Bounded gameplay HUD repair candidate

This candidate fixes the stale captured lower-court scoreboard in the current C port. The match clock already advanced: the baseline 7,200-frame run changed 43,200 to 36,796 while the captured BG3 panel remained WEST 2 / ORLANDO 0, 11:49. The candidate renders the indexed original HUD buffers using current canonical teams, scores, and clock, expires the panel, refreshes it on pause, and retains the original final-minute clock and shot-clock visibility rules. Normal human gameplay remains gated.

The candidate is a private snapshot of integration dc10166; no root source or production manifest was edited here. Integration 4707a16 already contains the matching asset pipeline; do not replace its portable extractor or upgrade helper with this candidate's earlier extractor copy.

## Source contract

The due-camera caller is 87:95DB -> 85:8E1C -> 83:CC10, after the credit wait/subject copy and before camera movement. Canonical 092A is sampled at the existing counterpart of 87:94A5. 85:EDAC decrements positive presentation timers only; CC10 separately owns zero-to-FFFF expiry. EBDB uses the actual clear table/ranges, including clock-column preservation below 3,600 ticks. 87:9578 consumes 09C6 and invokes BACB; 85:9437/9440 clears the shot icon and applicable prior panel before a foul event.

CE36 follows the original selector and shared 07F6 rejection-loop order. Shot categories 4939 come from 86:9DFB/A5AA/A9F7; assist 493D comes from the canonical scoring context at 85:A1C7. A made basket invokes CE36 at the 85:A346 counterpart. Native requests select kind 1, then kind 6 with RNG 9587 -> 38348 (one rejected draw), then kind 1. An unsupported statistics kind is never replaced by a fabricated score panel.

Pause uses 86:8352 to clear canonical 08E2 before 835D -> CE36. On typed pause/timeout return, B99A then BA54 resets visible HUD state while retaining working canvas/CHR, old clock text, generated published CHR/mask, phase, category, assist, and counters. C pause game-clock/actors/visible-output hold is tested; native paused 08DE identity or callback disable is NOT established. ED0D/EDAC callback scheduling and NMI timing remain outside this repair.

Preserved original endpoint behavior: BA5E increments before subtracting 60 and repeats on zero, so raw shot clock 60 selects frame 2 and 599 selects 10. CC6F uses the wrapped subtraction sign. BBE9 retains its original first/later 3600/3601 behavior and distinct snapshot/current-clock reads. These are not corrected into conventional rounding.

## Evidence and concrete limits

The original ROM SHA-256 is 2115c39f0580ce19885b5459ad708eaa80cc80fabfe5a9325ec2280a5bcd7870. Mesen SHA-256 is d2eb03c2590c648bf329f127ebcfefd70130e7690a9e2ccdba8616faea1fe96b. Fresh isolated native-lifecycle-v3 uses normal inputs only, no RAM/CPU/ROM writes; its manifest SHA-256 is ac0da740300c8afc39d76e7620319cbb34b79420fae23927826b30dc29c0aa74. Source listings and previous failed capture/build/decoder attempts are retained.

The fresh 9-source /W4 /WX native probe compares 746 before-only pairs / 2,747,930 owned bytes. Exactly 2,747,926 bytes match. The other four bytes are two observations of word 08F6: at outer CC10 and D1FD C reads 39945, native reads 39944 after a crossing interrupt. The actual inner BBE9 entry supplies the independent observed dependency, not C after-state input. native-verify-v1/report.json preserves the unqualified FAIL; native-verify-final-v1/report.json explicitly sets full_atomic_parent_pass=false. The bounded verifier accepts only those two independently identified dependencies when the explicit flag is passed. Visible VRAM upload/scanout differences are recorded, not asserted equal. CPU/DP/register execution, instruction timing, shared callback scheduling, and full NMI/upload-queue ownership are excluded.

Statistics rendering children (including kind 6 at 83:D333 and later original kinds) remain untranslated and expose pending_routine plus a stderr diagnostic. Gameplay and canonical timer continue, but the unknown child is not advanced as if implemented. Foul-kind clear continuation 83:EC60 is also explicit. Advertisement upload remains pending; its source selector/counter/RNG behavior is preserved without invented visuals. Those routes are not complete HUD parity. The implementation does not freeze the whole game waiting for them.

The fresh 40-source runtime journey has no score, clock, actor, or RNG seeds. It uses ordinary Start/Down/A at ticks 4500/4622/4624. It observes 120 pause holds, 182 formation holds, 7,630 dead-ball frames with clock movement, 1,942 dead-ball holds, and 32,001 live clock steps; final-minute images show 59.8 then 59.4. First-panel images show CHICAGO 0 / ORLANDO 2 at 11:32 then 11:31, followed by a clear court. A later image shows the updated 2-2 score. The file named second-basket-pending.bmp retains its older test label but the final v5 image is the actual later score panel. Pause BMPs are identical across the 120-frame hold. This probe calls actual Tipoff init/update/render directly, as the existing direct-scene CLI does; it is not a complete cold-boot/audio journey.

The fresh 40-source contract probe passes all 65,536 timer words and 655,360 shot-clock/game-clock combinations, 42 malformed map dimension/format cases, legacy-v1 production rejection without state mutation, reset preservation, and three clear boundaries. Native BA5E observation is absent from this particular route; controlled C tests and the auditor's separate original-opcode exercise do not expand natural coverage. The strict native verifier pins the capture, actual private settings/save artifacts, executable/pack/source identities, raw fields, clock mapping, CPU D0/M16/X16 domain, full response size and exact stdout/empty stderr. The final verifier has not received a new independent mutation-suite pass at this freeze; auditor acceptance remains required.

## Assets and reproduction

Resource 286 v2 is 3,926 bytes, SHA-256 4a39e5d5464b676eed999823c178e41f3251a1a830b96221cfd3d3a50c1c0f2d. It adds 11 direct-ROM indexed shot-clock tilemaps; all nine older sections are byte-identical. Initial 63 indexed CHR tiles remain original native-decompressor-attested; the retained format-30 attempt did not independently close the decoder. No RGB recreation is used. Production init requires the complete v2 domain before changing state. All map dimensions/formats are checked before access, fixing the old WIP malformed-resource over-read.

The 264-entry, 89,442,736-byte candidate pack SHA-256 is f564c29612928984002ed3f0389d317de639fff122baf61a7bc9ecaef2a6be09. pack-identity.json binds all 263 prior resource payloads and metadata unchanged. The accepted resource capture is C:/Users/joshs/Projects/nba-live-95-c-port/.analysis/worktrees/team-context/build/native-hud-default-v2 (manifest 68bf30adaa09c0cf8cde4c81439ec6572a758235218ccf7d0f3e9c4e30ba6812). Root 4707a16 provides tools/upgrade_gameplay_hud_pack.py and docs/gameplay-hud-assets.md, and independently produced this same pack at completion-owner/build/hud-integration-v1/nba95_assets.pak. Use that documented pipeline; old 263-entry packs and legacy resource286-v1 are deliberately refused by production init.

Run commands and exact compiled source/header/executable identities are in the packet build manifests and HANDOFF.json. Rebuild into new private output directories (the retained build scripts deliberately refuse existing outputs). The baseline and first-panel-pause-attempt-v3 freeze remain immutable. The user's latest gallery is unchanged; root will refresh it from integrated sources after review.
