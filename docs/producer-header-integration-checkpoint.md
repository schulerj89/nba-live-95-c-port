# Accepted bounded producer/header source components

The independently accepted producer/header revision2 composites are copied
byte-for-byte into the integration worktree. They remain standalone components:
the production source manifest, scheduler runtime, human-play gate, primary
checkout and desktop build are unchanged by this checkpoint.

Scheduler producer freeze2 SHA256 is
`0f784ce695df33e3c4dca126e94865b8ce582d13e865e3408c65b7108fe5e39e`;
header freeze2 is
`0f527db9a72689641ca702dc379314c9a49cb6ce82ba2383db354f4e400790dc`.
All51+49 referenced identities were checked before copying. The root copy
ledger `build/producer-header-integration-v1/copy-identities.json` binds49
source/audit files, including13 already present and36 new files.

Fresh root `/W4 /WX` builds and explicit read-only scheduler native inputs pass
both source-work proofs: producer155750 instruction states,40003 ordered CPU
writes,27726 DMA bytes/73 jobs and four550560-CPU intervals including caller
JSL; header126 states,66 writes,8206 bytes/three jobs and four440-CPU intervals.
Each component also passes its13 Python/11 C contracts, nine local corruption
cases, and the unchanged independent five-case chronology/PC mutation suite.
The ten shared protocol tests pass. Commands and exit codes are recorded in
`verification-runs.json`; every proof, mutation report and log stays beside it.

The original rejected verifiers, captures and rejection reports remain
unchanged. New v2 wrappers require original chronology and scope/source-PC
association before composing native streams. Existing source continuations
and their original width, wrap and callback-order behavior are preserved.
Capture runner/Lua bytes are pinned against checkout newline conversion.

Independent acceptance is
`completion-producer-header-verifier-v2-independent-audit.md`, SHA256
`9d3cdc86b6de2b4973b61645e9c128cce9bd4b17b3e825fe68e8dc6709874e39`.
The older source documents' pending/rejection statements remain historical;
this document records the later accepted composite and owner reproduction.

No elapsed DMA timing, carried scheduler phase, interrupt/audio/SPC closure,
wait epoch, Rules reentry brightness/RGB or whole-game parity is established.
The sound prefix and normal sound initialization remain separate work.
