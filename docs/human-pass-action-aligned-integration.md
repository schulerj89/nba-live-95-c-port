# Accepted action and aligned-pass components

The independently accepted action/aligned source slices and their separate
v2 verifiers are copied byte-for-byte into the integration worktree. Root
checked121/146 original references and96/108 verifier-repair references,
copied33 source/audit files, and rebuilt both probes against current owner
objects. The production source manifest and human-play gate remain unchanged.

Owner replays pass64269 action values and129935 aligned values across the
original left/right captures. Both local mutation suites pass55/70 cases;
the unchanged independent ten-case protocol suites reject all ten for each
component. Reports, commands and source-copy identities are in
`build/action-aligned-integration-v1`. Fresh probes are in the separate
`build/human-pass-action` and `build/human-pass-aligned` directories.

Independent acceptance is recorded in
`completion-human-pass-action-aligned-verifier-v2-independent-audit.md`,
SHA256 `559bf5787b8df8e79598080a9e38485fbf6042ffe56d5ed3642f061cf651d5b6`.
Original rejection reports and v1 source/capture/probe files remain intact.
The repairs only enforce integer-zero process status and the exact expected
asset-loader diagnostic; native expected values and C behavior are unchanged.

Action covers the AC50 gate, grounded B00B branch and bounded upper-animation
child. Aligned covers AD0E gates and AE10/F473/installation through explicit
continuation boundaries. The original lock/request order, wrapped subtraction
and ordered/asymmetric lane scan are preserved and commented by source PC.
The aligned field named `options_07f6` is the actual raw `$07F6` PRNG word,
not a UI Options field. Its numeric ABI is retained for these frozen proofs;
do not bind it to configuration when integrating production callers.

The three naturally witnessed F473 calls are blocked-lane outcomes. Clear,
endpoint and extreme/borrow cases are independently checked source controls,
not naturally witnessed coverage. Catch, pose/attachment, final stack return,
full initializer completion and ordinary human input journeys remain separate.
No rejected host-verification behavior is treated as an original-game bug.
