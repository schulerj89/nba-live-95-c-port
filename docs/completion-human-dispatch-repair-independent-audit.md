# Independent acceptance of the bounded human-stage repair

2026-08-31. **PASS for the repaired bounded stages only.** Original human freeze v1 remains rejected with unchanged evidence in `completion-human-dispatch-independent-audit.md`. This accepts root's separate repair freeze; it does not enable production human input or accept the unimplemented action children, other-button continuation, behavior dispatch, or complete human gameplay.

Root freeze: `completion-owner/build/human-repair-freeze-v1.json`, SHA-256 `f4b6f4377e377f5d92d2eca410cdea6e498a216abe91feff24c640a36dfdb017`. All 27 file identities and all 40 recorded object identities were independently rehashed. The auditor snapshot and fresh results are in `completion-auditor/build/human-audit-v2`; original source/captures remain unchanged.

| Reviewed source | SHA-256 |
|---|---|
| Human module | `097bc85190367d6ae803026f3e5c217ff5aa53a9e20b78a911fd5b766bcccc5f` |
| Human header | `9b05b6459264e09c021447a7632a13b9767fb33cd6c86746ff2c49d0fc33fc92` |
| Human probe | `cfe5141e6b89cfabd6b94be11cec1afb94f7e2920ede32926a2815e9a8cc40ef` |
| Human verifier | `979e9b35619ecd989dffc918dd354dbdc61fcc7e16e0805833a7cf47b4cc7203` |
| Accepted shared isolation verifier | `eb44afe38217dfcd7dfdfb2432434dda87d5ef439e76ee75eaa84b4778b0e0db` |
| Unchanged gameplay velocity source | `94cce032be9023edc6f93ba4d7d80bd91568b8e6d97a3ffe70909b52838fd9ea` |
| Fresh auditor probe | `166457e1696f6da904fa3d71150970d1af0825f97ba85215335c1d177cc74785` |

The module/probe were freshly rebuilt with `/W4 /WX` in the auditor directory, linking the auditor's previously fresh exact accepted controller-checkpoint objects read-only. The root's frozen executable was not used as the acceptance binary. The unchanged global velocity source was compared byte-for-byte to that independent checkpoint.

The repair selects `controller_word_72` only when the source's wrapped live compare is negative and the full actor Z word is nonzero. Otherwise it retains the actor timer. Earlier gates, accelerator-call observation, velocities on blocked paths, and the helper's wrapped subtract/sign clamp remain intact. The source now explicitly comments the original carried-X bug and its natural witness. Blocked calls correctly avoid validating an unused direction vector. No global helper behavior was changed.

The probe publishes the referenced word separately, including controller 4's `$495D` word beyond the five-record vector. An independent controlled pad-4 input confirms referenced 7 becomes 5 while actor timer 5 remains 5. This demonstrates C address/projection coverage, not natural pad-4 reachability. `additional-source-checks.json` preserves it.

Fresh native prestate replay:

| Unchanged capture | Compared values | Result |
|---|---:|---|
| Original selection 0 | 337904 | PASS |
| Original selection 2 | 367653 | PASS |
| Auditor ordinary L+X diagnostic | 59093 | PASS |
| Total | 764650 | PASS |

All original 703,914 fields remain checked; the added scalar contributes one value per motion call. The original 29 frame-crossing calls and the diagnostic's two crossings remain present. The three natural timer divergences are now gone. Native fixture bytes, original ROM and input scripts were not changed for this repair.

The unchanged independent 25-case provenance/output tool passes every case. The unchanged independent 12-case alias tool passes every case, including full-width Z, wrapped compare, borrow versus sign clamp, zero delta and actor-target controls. The root's separate 14-case quirk probe was inspected, freshly compiled and rerun successfully. Verifier source review confirms exact command/route fields, typed range/row domains, ordered frame/court observations, known runner identity, complete sparse-boundary declarations, and validation through the accepted isolation routine without overwriting recorded observations. The additional referenced-word output participates in the same exact-field and integer comparison as the original fields.

Acceptance remains a native-prestate replay of three source stages. Native action children supply the motion stage's post-action inputs; those children are not implemented or approved here. All exclusions and caller preconditions from the original independent audit still apply. The accepted controller checkpoint's intentional production human gate remains in place.
