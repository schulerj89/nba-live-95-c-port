# Accepted mode15 pass-release components

Root rehashed all218 frozen identities, copied14 reviewed files, and freshly
compiled the nine source dependencies under MSVC /W4 /WX. Both native routes
pass517 calls across30 pass origins, comparing2,878,464 values. All126 local
and44 independent malformed-evidence cases reject with the fresh executable.

The accepted boundaries cover the real mode15 wrapper, animation gate,
family5 facing turn, waiting attachment, launch selection and separate return
cleanup. The original wrapped-family and facing quirks remain commented.
The99C4 launch itself is still excluded, and its return component receives
its own actual native prestate. The final right-side timer remains unfinished
at the capture end; only29 normalizations were observed. No human-play enable
or production caller change follows from this component acceptance.

The first root runner passed both native routes, then passed an unsupported
--assets option to the local integrity test. That command failure is retained.
A separate retry uses the tests' declared arguments and passes all170 local
and independent cases. No reviewed source, capture or expected value changed.
Root records are under build/release-integration-v1; the independent review
is in completion-human-pass-release-independent-audit.md.
