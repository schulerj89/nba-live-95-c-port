# Independent human pass selection review

FAIL for acceptance of pass freeze-v1, SHA256
`2e66c85ccfff9353e9588c148dd9aab1079d65c856284af85b29adaaddb7211f`,
because its verifier accepts contradictory or impossible event metadata.
The new selector and F1C1 distance body pass the inspected source contract,
fresh captured-input comparisons and bounded independent arithmetic/quirk
checks. The existing directional F34F dependency has the separate wrapped
comparison defect documented in the switch audit. This pass checkpoint
explicitly limits its previous F34F acceptance to ordinary captured court
inputs; it must not acquire a broader claim from the passing new metric.

All71 frozen source/dependency/object/artifact identities were independently
checked before work and remained unchanged afterward. The eleven new files
are copied exactly under `build/pass-audit-v1/source`. Fresh MSVC /W4 /WX
module/probe compilation used the auditor's earlier freshly built accepted
controller objects read-only. Fresh probe SHA256:
`1590054fae27828b8cbc79c28707e6e1e7411e0143089a5a57764d7b08c163b1`.
Both untouched native captures pass25 receiver selections and41 F1C1 calls,
42,766 compared values. The left entry124/court1471 frame crossing remains
included. All four F1C1 return PCs are witnessed across the pair. Original38
local evidence tests pass. No new native journey was claimed by these fresh
C replays.

The new source identity is
`f149d20f8bcf6ee18aaf9328d80aa56f8f3c9c9b90b7674a2b3e71462112342f`;
header `f8f85a027eae7e3be3cef85244214e0b1157bef792451d98f45fe94378d3122f`.
The actual original ROM is SHA256
`2115c39f0580ce19885b5459ad708eaa80cc80fabfe5a9325ec2280a5bcd7870`.
All five reference byte ranges and ten reference artifacts were rechecked;
I read the Ghidra instruction listings, recompiler boundary output, module,
probe and actual Lua/runner. DF7A-E141 bytes hash to
`2bc624a44f1d67ef434f358c7bde3381c6fa07a23490e7f5b752ffa7dfdc5f34`;
F1C1-F229 to
`3774e5b5b071d2e8bead32614dfb306c7db2aa4dc105287dd2cc29ed95705e30`.

F1C1's absolute-word magnitudes, wrapped CMP/BMI branches, doubled minor
coordinate, intermediate add wrapping, right-shift truncation and distinct
3/8 versus1/4 weighting agree with the instructions. A fresh direct C metric
probe compares576 boundary pairs against literal instruction arithmetic,
including8000/7FFF, equalities and doubled sign changes: all pass. This does
not establish natural reachability or exhaustive arithmetic coverage.

Nine controlled selector cases pass: no receiver still publishes090E|10;
exact1600 acceptance still skips the initializer; later directional and
earlier neutral ties; the original suffix preference; and signed wrapped mode
gates atFFFF/8007/8006/7. The 8007 case is accepted because8007-7 has its sign
bit set;8006 is rejected. Initial source-contract-v1 accidentally called the
accepted8007 case "rejected" in its label while its expected/actual values
were already correct. The corrected v2 report adds the adjacent8006 guard
and preserves the original report.

The suffix behavior is an original quirk, not a simplification to fix. At
E0B5-E0E2 the preference scan starts at the current candidate. Once a favored
earlier candidate leaves that suffix, a later less-favored candidate can win.
The source comment records this accurately. I independently read the raw
entry actor+8C values and actual accepted candidate/score events for all four
documented natural left-route witnesses:17/76/106/138. Entry17's source281,
accepted161 then351, and scores120 then69 agree with the untouched artifacts.
The E085/E088 score-equals1600 quirk is also retained/commented, with controlled
coverage only. BRA E03A skips the alternative E03C-E081 block; the API does not
invent it as a DF7A route.

The C probe receives only each selection or metric entry. It never consumes
the native initializer result. At AB2D entry it compares route, score, receiver
identity/pointer, all1408 actor/ball words,160 controller words,128 context
words and nine global words. The native initializer return and DF7A epilogue
are observed separately; their after-state is not projected into C. Saved
caller restoration is a native integrity observation, not implemented child
behavior. Volatile scratch/CPU registers are outside this semantic stage;
joining a future initializer must establish its complete calling contract.
Neither the source manifest nor a production caller enables these modules.

The blocking verifier is `tools/verify_human_pass.py`, SHA256
`30856f3bd8017f5e409fccb60340bfc29e102661af082e03be0810909e8881e7`.
Independent39-case tool `test_human_pass_integrity_audit.py` rejects24 but
accepts15 invalid parsed event views: six16-bit fields at65536, seven raw entry
field disagreements and court/frame clocks shifted by100000. The source
capture writes words, while the verifier grants those fields a24-bit domain;
it also does not cross-check raw event metadata against its own snapshot.
The field mapping and clock repair contract are the same as the switch audit.
These tests leave original capture bytes and normal artifact identity checks
untouched. Exact source versions, process command/environment, isolation and
typed C responses do reject their targeted mutations. Complete candidate
scan, pairing and epilogue checks remain enabled.

Evidence is under `build/pass-audit-v1`: freeze/reference rechecks, both fresh
native reports, `local-tests`, `source-contract-v2/report.json`,
`natural-suffix-quirk-recheck.json`, and `independent-integrity-v1/report.json`.
Direct arithmetic tools are `human_pass_distance_audit_probe.c` and
`test_human_pass_source_audit.py`. Original freezes and rejected evidence stay
unchanged. This review does not accept AB2D execution, complete human passing,
other buttons, production dispatch, or the full human-enable gate.
