# Independent pass verifier repair review

PASS for the verifier-only repair at
`completion-controllers/build/human-pass-verifier-v2/freeze-v1.json`, SHA256
`57ec2da5ad270b55eea9ec5484f8fd49ad6495c8509e88fcaa70fbd9744702a4`.
All61 source/dependency/artifact/prior-freeze identities were independently
checked. The three new files are snapshotted under
`build/pass-audit-v2/source`; original pass11 C, probe source, original verifier
and native captures remain unchanged.

New verifier SHA256:
`3dadecb7be59ecbefec1f76a30c5e89b6fe488d6e07c7f295a31b5b52130ce79`.
The diff repairs source identity shapes,16-bit word domains, raw-event field
consistency and clock validation. DP C2 remains correctly understood as the
capture's diagnostic actor label. Raw direction comes from090C+6 or the
originalFFFF fallback. Player2697 and court4590/4390 anchors belong to the
exact pinned capture/runner/settings and input routes; these are evidence
checks only, never production timing. Existing scan counts, paired metric and
initializer boundaries, exact C comparisons, epilogue observations and
source/isolation integrity are retained. No native expected values change.

Fresh replay with the auditor's independently built original pass probe
`1590054fae27828b8cbc79c28707e6e1e7411e0143089a5a57764d7b08c163b1`
passes both captures:25 selectors,41 metrics and42,766 compared values.
Its stdout/stderr match the previous original-verifier replay byte-for-byte.
All four metric exits and the left1471-to1472 frame crossing remain included.
The unchanged39-case independent integrity tool now rejects all cases,
including the previous15 accepted contradictions; the new local62-case
rejection suite also passes. This is bounded integrity testing, not exhaustive
proof of every intermediate diagnostic row.

I additionally recompiled the unchanged pass module/probe into a new private
directory using the independently accepted repaired direction-helper object.
This composite probe SHA256 is
`409892c7481d7e8f1180626fbd7b3edd83848dcb6498a7516e5a58672f87d935`.
Both native comparisons pass with byte-identical responses to the old-helper
replay. The helper's576 full-word arithmetic checks and controlled switch
counterexample are covered by the separate switch repair audit. The original
new pass metric/selector's576 arithmetic and nine controlled quirk checks
remain in the preceding pass source audit, unchanged.

The accepted scope is DF7A receiver selection through the initializer entry
and the separate F1C1 result. AB2D execution, later pass actions, production
caller scheduling and the normal human-enable gate are not accepted here.
Original suffix preference and exact1600 no-pass behavior remain preserved
and commented. Native fixtures and earlier failures are untouched.

Evidence in `build/pass-audit-v2` includes `freeze-recheck.json`, both
original-helper and fresh composite replay reports, byte-identical probe
responses, fresh composite build log, `independent-integrity/report.json`
and `local-tests/report.json`.
