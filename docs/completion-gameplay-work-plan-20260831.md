# Controller/gameplay execution plan — 2026-08-31

Read-only planning baseline: `completion-owner` commit `f318478ef12586928c2e03b034a49ddf8bc508bd`. No production edits, captures, archive-source merge, or implementation claim in this planning pass. The archive commit `b8bbbcd` is historical evidence, not an integration base. Future tickets below include implementing/fixing and runtime wiring, not merely investigation. Controllers agent implements; root owns integration/manifest changes; a different agent audits each candidate. Native runs are serialized with the scheduler agent.

## Current facts that determine order

- `build/hud-runtime-integration-v1/cpu-first-failure.json` identifies frame **41876**, actor0: pass active/unreleased, receiver4, mode8, saved mode15, animation0x35. Band6 and saved-mode aliases agree. `tools/test_cpu_gameplay.py:1630` rejects the animation because it allows only pass poses while unreleased. This is not yet a proved implementation defect or a proved bad assertion. The current knockdown path in `src/nba_tipoff.c` installs 0x35/mode8 and can drop ownership; it does not establish the original fate of all pass globals.
- The special receiver branch of `cpu_owner_flow_call` skips **86:B468**, sets mode14, and copies velocities. Original **86:AF66 -> AF83/B468 -> AF87/AF8F** first supplies a pass-band timer, calls the child with exchanged pointers, restores them, then installs mode14/flags. B468 owns +56 atB612 and +58 atB61A. Merging aliases before that producer exists exposes stale target coordinates as mode selectors.
- Live `match_restart_period` still recomputes the clock, writes09C6/13F7, and uses `cpu_begin_dead_ball`. Accepted **86:DCA6-DD97** plus typed **DD97-E207** components are outside `nba95_sources.txt`. Whole restart is not fixed by their standalone acceptance.
- Human allocator/input/transfer is integrated, but init still forces neutral CPU behavior. Accepted dispatch/switch/pass components remain dormant; launch99C4's code evidence is positive but its natural and controlled F78B return-route verifiers still need the separately requested repair/audit. A complete B pass is not complete shooting/defense/human play.

## Four work tickets, in execution order

### C1 — Resolve the interrupted-pass regression without weakening its invariant blindly

**Deliverable:** source-correct contact/pass cancellation or preservation in the actual actor loop, plus a corrected invariant only if the original permits the observed state. Preserve the existing failure, trace, binary and assertion first. Use compact C trace rows around41876 to identify the first writer/order, not only the failed endpoint. Trace **86:BFBA-C217** knockdown and, if actually selected, **86:C91E-CB83**, the mode8 recovery continuation, and pass cancellation **86:A60D/A613** versus mode15 release/return. Existing source comments identify candidates; actual triggering branch remains to be established.

**Work boundary:** `nba_tipoff.c` contact/pass adapter and `test_cpu_gameplay.py`; no HUD RNG rollback, forced pass release, or arbitrary owner reassignment. Replay source contracts for saved mode84, mode5E, action28, pass09C4/0942/0946 and animation locks. If original preserves the pass metadata while mode8 is active, scope the pose invariant to the actual executing mode and assert preservation/recovery separately. If it clears data, add precisely those missing stores at the original boundary.

**Gate:** fresh `/W4 /WX` build; retained41876 reproducer before fix; original-ROM bounded entry/exit evidence plus controlled interruption cases; normal current-pack CPU journey through at least63800 frames. Every new first failure is retained and attributed rather than skipped. A normal native contact witness is required before claiming normal-route parity; a controlled state replay alone is labeled as such. Re-run HUD live score/clock/pause tests and new-match reset. No expectation that restoring original RNG leaves old scores/frame numbers unchanged.

**Risk:** source may preserve stale-looking pass metadata intentionally. This ticket must distinguish a test-domain error from a port bug, and must not declare the HUD causal merely because its restored RNG makes the route reachable earlier.

### C2 — Implement the receiver producer, then unify original shared actor words

**Deliverable:** a typed **86:B468 through its actual return** implementation shared by the receiver and any proven compatible shot caller; integrate the full **AF66 -> B468 -> AF87-AFA5** receiver sequence. Supply the original pass-derived +60 and actual pointers/geometry/profile/RNG; do not call `cpu_start_rom_layup`, which initializes its own timer. Derive all reached children/tables and preserve **87:B7D8** shared-RNG ordering. Preserve AF6E's six-byte-band table access, including the source-only band30 opcode-byte result; do not invent a sixth row.

**Ownership change after producer proof:** consolidate original +56 (`target_x`/`special_contact_raw_56`), +58 (`target_y`/`mode13_variant_raw_58`), and +60 (`reaction_threshold`/`contact_action_timer_raw_60`) into one authoritative word each, with explicit signed/unsigned access at each source reader. Inventory every writer/reader in contact, mode13/14, CPU planner, pass and period adapters. The rejected private alias experiment's first difference atframe306 is a regression target, not an expected native state. Reconcile09DA..09EC as one shared assignment/pass/role buffer when implementing its actual producers; do not duplicate a second authoritative distance.

**Gate:** original source/Ghidra/recomp agreement; before-only B468 native child and AF66-parent capture with ordinary pass input if naturally reachable; all resulting velocity/baseline/selector/timer/animation/RNG words compared. Controlled band, sign, carry and profile cases separately labeled. Fresh CPU normal run and the C1 interruption checks before and after alias migration, plus accepted catch/pose/return native suites. No broad alias merge accepted from a7200-frame smoke test alone. A missing child is a visible bounded stop until implemented, never a guessed result.

**Dependencies/risk:** C1 attribution first so alias changes cannot conceal that failure. B468's alternate entry/caller preconditions and reused scratch are the main source risk. This unlocks both period canonical state and the human special-receiver branch.

### C3 — Wire the accepted restart into the real match lifecycle

**Deliverable:** replace legacy `match_restart_period` with typed current-state adapters for **87:976E/9797 -> 8C86 -> 86:DCA6-DD97 -> DD97-E207**, using `nba_period_entry_prefix` and `nba_period_formation` plus their real children. Root coordinates the production manifest and scheduler/draw-order ownership; controllers implements the NbaTipoff mapping and fixes failures. No old complete initializer, checkpoint image, child-poststate callback, or generic dead-ball substitute.

Before wiring, establish carried0A0C period length, context28 distinct from2E, published pose34/36/3E/40, complete channel queues, canonical controller actor16/context3B/3D, 24 roster pointers, collision sentinel/list,12-record draw permutation, basket3FEF/3FF3,084A/084C and source dispatch deltaC6. Keep one owner for07F6 and09DA..09EC. Preserve lower native position fraction bits: the current eight-bit host fraction is insufficient for an arbitrary16-bit carried fraction; choose an explicit retained-word adapter/storage extension, not rounding. Ordinary **80:FC80** is one pass; period **80:FBFF** is full sorting. Scheduler agent supplies the former runtime owner; do not duplicate it here.

Preserve original carried09BA,09B0/09B2, player fractions, pair-index carry and old basketX through period2 anchor flip. Remove only old host resets with no source writer (including the existing inappropriate09C6/13F7 restart stores). If current state reaches the typed role component's **BF51/BAE4** or unsupported appearance domains, implement those exact reached children before accepting that route; do not force a permitted state or ignore REFUSED/ROLE_STOP.

**Gate:** all existing four controlled-expiry formation captures and125before-only checkpoints still match after mapping; poisoned alias/carry/queue/fraction round trips; fresh naturally elapsed CPU Q1->Q2 and Q2->Q3 with clock, inbound ownership, actual movement and source-appropriate visual/HUD transition. Then regulation->Q4, final, tie->OT and repeatedOT gates; controlled expiry may accelerate diagnosis but does not replace natural elapsed-period proof. Re-run C1's63800-frame test and run a complete CPU game through final/new Exhibition. Stop evidence explicitly at any unowned scheduler/UI boundary; no guessed delay to make a screenshot match.

**Dependencies/risk:** C2 shared-word ownership and root/scheduler's persistent draw-order owner. This is an integration ticket built from accepted modules, but additional live-only role branches are separate child closures if encountered. Native CPU/NMI timing remains a distinct release gate, not supplied by the typed formation API.

### C4 — Compose the human caller and deliver the first real B pass/switch/movement route

**Deliverable:** a continuous, before-only human actor dispatcher and production adapter, initially behind a development gate. Repair **99C4 launch** natural AND controlled companion verifiers in new files first: F78B same entry A/X signs must returnF820, opposite signsF7AE; derive return PC/status from that source route, not the claimed tag. Re-run the auditor's unchanged forged-return cases. Preserve launchv1 and all prior freezes.

Compose **87:9106-929E**, including still-missing requester **9165-91BF** (Start/Select/free-throw preparation), input once-per-actor/processed marking, action return, physical actor commit, and post-action movement in source order. Wire **84:E2AC -> DF7A/E141**, the accepted AB2D prefix/action/aligned/catch/pose/return pieces and mode15 release->99C4->return using C-produced intermediates only. Implement C2's B468 continuation and any actually selected remaining AB2D branches (e.g. ACA9/AFC4, B3BD) rather than using the historical whole initializer with retained mismatches. Use canonical home/context0/group0 versus visitor/context1/group5 and physical pad identities; restore original team-preserving BC9B ownership transfer. Preserve the documented carried-X controller+72 movement bug exactly.

**Gate for this first ticket:** normal fresh-menu left and right selections plus neutral control; offense pass, defensive switch, held/released directions/boost, inbound B, contact/interruption/recovery, pause/resume and free-throw transition. Capture/replay the continuous caller from controller publication through actual actor update and released ball/receiver, with no afterstate seeding and exact shared RNG/control/animation/ball ownership. Fresh strict source/ROM/exe/input/settings/raw/clock/domain/output attestations, source edge cases and independent verifier mutations. A dormant leaf replay is insufficient.

**Complete-human follow-through, not hidden in this ticket's DONE:** derive and implement each remaining **84:E2F2-E3E9** non-B branch (shot/jump/steal/block/other controls as the original branches determine), forced movement/control-lock continuations, free throws, lifecycle reallocation and additional pads. Use separate implementation-sized child tickets at the exact discovered calls. Then run a whole human match on each team, all buttons held/pulsed/released, pause/substitution/inbound/FT/period/final/new-match, plus neutral CPU regression. Only that broader action matrix and routing audit permits removing the production neutral policy. Completing C4's B route alone does **not** enable or certify complete human gameplay.

**Dependencies/risk:** C2, launch-verifier acceptance, and stable period ownership for whole-match testing. Leaf APIs use different pointer/index names and aliased words; bind original offsets, full words and actual pointer address spaces, not coincident host field names. Unsupported branches must remain explicit until implemented.

## Parallel execution and Max use

Controllers begins C1, then C2; root/scheduler can work on the persistent draw-order and period-owned producer prerequisites without touching the same tipoff adapter. C3 follows their accepted ownership and C2. Auditor reviews each freeze while the implementer prepares the next independent slice. Root alone integrates and refreshes current-port screenshots after meaningful accepted changes.

No immediate Max consultation is needed: the retained41876 case is a concrete source-attribution task, and B468 has an identifiable missing producer. Escalate a narrowly evidenced blocker to the **existing** Max consultant only if the raw ABI/pointer/carry analysis still admits incompatible parent compositions, or if retaining16-bit fractions plus current actor integration cannot be represented without changing untouched native-owned words. Supply exact original PCs/bytes, before/after raw states, both competing mappings, and the smallest failing contract; request a decision/review, not a new speculative implementation. Do not create another consultant or use Max to replace ordinary source work.

## Read references and scope

Current root: `docs/gameplay-hud-integration.md`, `period-tipoff-state-mapping.md`, `period-formation-typed-composition.md`, `period-formation-integration.md`, `period-entry-prefix-integration.md`, `period-render-integration.md`, `period-components-integration.md`, `human-dispatch-repair.md`, `human-pass-integration-checkpoint.md`, `human-pass-release-integration.md`, `human-pass-catch-checkpoint.md`, `controller-ownership-model.md`, `ownership-plan.md`; current manifest and actual contact/owner-flow/restart code. Older integration notes explicitly describing earlier boundaries are not treated as current acceptance summaries. The complete release plan also needs frontend/modes, rules/events, statistics/replays, persistence, rendering and audio; these four tickets do not certify those independent workstreams.
