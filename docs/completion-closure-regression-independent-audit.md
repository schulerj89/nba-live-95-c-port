# Closure driver and C digest independent audit

Verdict: **PASS for the released Setup-input migration, Main-row assertion and attributed C-only digest**. This direct component journey does not establish native scheduler phase, Rules reentry, normal human gameplay or a complete game.

Root `build/closure-regression-freeze-v1.json` SHA256 `12956e85a3f44e0097a798bd79dd8bb38c0483b6327977f93f86a8c418dfbe38` binds 2,378 independently rehashed files (2,960,868,754 bytes). Supporting source/build provenance is the separately accepted gameplay85 and startup-fixture freezes. Exact probe copies, fresh private builds and comparisons are under auditor `build/closure-audit-v1`.

## Driver and assertion scope

The actual original/final probe diff adds one zero-held update before each requested Setup press, changes those calls to use the helper, replaces the forced post-Options row assignment with a row0 assertion, and updates only the attributed expected digest/comment. The original four/five Down counts, action/page/sound/value/commit assertions, transition waits, Team Select/Player Setup/introduction sequence, 6,000 gameplay updates, hashing, repeated-run equality and all semantic minima remain unchanged. No configuration assignment is added to the checked-in journey.

The helper's released word reaches the actual Setup input producer, followed by the intended held/pressed word. Independent current UI records show four successive Main moves at frames76/78/80/82, actual Rules return to row0, five moves at370/372/374/376/378, Options return to row0 and the final Start. Press records do not claim to log every idle frame; the source supplies those released updates explicitly. The old direct held-Down driver is freshly rebuilt against the fixed startup source and still fails code12 with no transitions. The historical-navigation compile switch already existed; the reviewed normal builds do not enable it.

Factory sample0 has rule0=0/style0. The real adjustment yields rule0=1/style2 and the Options edit yields option0=29. The actual closure teams remain away28/home19. The separately accepted startup fix changes only synthetic test data and is required for that valid home roster; it does not replace the selected team.

## Independent reproduction and evidence checks

Fresh private MSVC `/W4 /WX` probe builds use the auditor's fully rebuilt, matched gameplay85 object sets. Alternate canonical tipoff files were checked against their Git sources and differ only by the accepted local startup fixture copy. The pre-layout helper retains the accepted F34F arithmetic repair. Seven migrated controls plus the original driver were independently rebuilt and rerun; every aggregate/counter line matches the frozen root record:

| Fresh source/configuration control | Aggregate digest | Outcome with its preserved oracle |
| --- | --- | --- |
| 52c2899 + historical tipoff / legacy | `fdbdd69c21271f89` | Original expected digest passes |
| Same / factory | `7f99d2502faa1c5d` | Retained digest-only failure81 |
| 52c2899 canonical / legacy | `51e05fa9d83da3b1` | Retained digest-only failure81 |
| 52c2899 canonical / factory | `43120bb45e70aa8b` | Retained digest-only failure81 |
| Current with pre-layout helper/tipoff / factory | `43120bb45e70aa8b` | Retained digest-only failure81 |
| Current / legacy | `ea1707afe4948974` | Retained digest-only failure81 |
| Current / factory, reviewed new oracle | `d26e6deec1fdc18e` | Pass |

The current final run also freshly emits all diagnostics. Every one of its 276 files is byte-identical to the frozen current evidence. The result retains eight transitions, 65 render changes, 2,910 motion frames, 13,646 resource changes and 71 possession changes. The two complete runs agree.

Independently checking the frozen matrix, each of eight cases has exactly the same 138 filenames and bytes between its first/second runs. This includes owned state, sessions, full telemetry, five-field gameplay projections, pixels/BMPs and UI records. Cross-revision comparisons use the declared semantic projections and pixels; different structure layouts are not treated as byte-equivalent.

The primary historical control and 52c2899/historical-tipoff/legacy control match all 66 raw pixel samples and all 6,000 gameplay projection rows. Thus the release-frame migration and compatible candidate pack can still reproduce the old oracle exactly. Historical-tipoff legacy/factory differs only at Main/Rules samples0/1, with every gameplay projection row unchanged. The canonical 52c2899 factory and current pre-layout control match every sampled pixel and all projection rows. These comparisons were recomputed directly from the files, not accepted from the implementer's summary.

The current pre-layout/fixed full telemetry is identical through index2304. At index2305, the independently calculated first difference is exactly six values: play12→16, inbound target(-377,224)→(-394,119), direction4→2 and actor2's corresponding target pair. RNG, ball, owner, clock and every other telemetry value agree there. Only pixel samples36–65 change; all sixteen pre-game and first twenty gameplay samples remain equal. This corresponds to the already accepted original C39C layout1 constructor repair, not a newly inferred renderer defect or original-game bug.

## Limits and retained evidence

The historical configuration override exists only in labeled private control probes. It was independently diffed as the declared Simulation/three-minute/rules prestate change. The original Rules44 native image fixture remains untouched; no exact-native image claim is made for the current factory Rules1 sample. The test still directly controls the long initial clock and RNG seed after initialization.

The earlier code12/code60/code81 failures and the old golden remain preserved. Fresh source builds and byte comparisons support this C regression update; they do not justify unrelated baseline changes or production enabling. No original native fixture, asset pack or gameplay source was modified by the closure patch or this audit.
