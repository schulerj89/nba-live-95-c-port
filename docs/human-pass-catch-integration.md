# Accepted bounded human catch component

The original AD3D catch geometry, indirect read, RNG, F02D distance and ordered
F5E4 lane scan are independently accepted to AE10. A source-only AF66 branch
stops before B468. No complete initializer, pass launch or human enablement is
claimed. Actual indirect WRAM-mirror reads, wrapped arithmetic, asymmetric scan
stops and the source-only band30 table overrun remain preserved and commented.

Root rehashed all287 freeze identities, copied the reviewed files byte-for-byte,
rebuilt the new component and probe with MSVC /W4 /WX, and pinned the38 current
objects used only for the existing asset loader/link dependencies. Three left
native calls reproduce31,788 compared values. The right capture has no catch
entry and is explicitly observation-only; it invokes no C comparison. All93
local and24 unchanged independent malformed-evidence cases reject.

The independent source audit additionally checked70,700 controlled ROM/C cases
across336 source PCs; this is source-contract coverage, not natural reachability
or an emulator timing claim. Root reproduced the native and protocol checks.

The initial audit inventory counted282 entries, omitting three external and two
private-object entries from its explicit inventory. Root's287-entry check caught
the count discrepancy. The auditor then explicitly rehashed all five and issued
the corrected report copied as `completion-human-pass-catch-independent-audit-v2.md`.
The original282-count report is retained unchanged. Acceptance and source code
did not change. Root copy/build/replay records are in `build/catch-integration-v1`.
