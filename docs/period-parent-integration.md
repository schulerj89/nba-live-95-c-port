# Accepted standalone period parent v2

Root rehashed all809 frozen identities, copied14 reviewed source/audit files,
and built the v2 parent/probe fresh under MSVC /W4 /WX. Four native cases pass
70 boundaries and28420 typed-word comparisons. All56 local checks pass, the
unchanged independent ROM diagnostic matches20 controlled cases/1600 fields,
and all10 independent metadata corruptions reject.

V1 had a port error in opening/OT formation with a positive anchor: it failed
to negate Y. V2 follows DDE7-DDED and also strengthens the caller metadata gate.
The original rejected freeze and independent report remain available; the
rejected C version is not copied into the root source directory. The native
cases do not enter that corrected branch, so their eight input/trace files
remain byte-identical to v1.

Confirmed original behavior is preserved and commented:09BA and09B0/B2 survive
the period path, actor coordinate fractions carry through formation, and the
loop writes pair index0..4 into+A6 for both teams. Those are not repaired as
port bugs. The new helper is not in the production source manifest yet. Its
explicit appearance/sort/controller/pose/role child boundaries still require
real implementations before the period caller can use it. Neither whole-period
transition nor native hardware timing is accepted by this component proof.

Root verification records: build/period-parent-integration-v2. Independent
acceptance: completion-period-restart-v2-independent-audit.md.
